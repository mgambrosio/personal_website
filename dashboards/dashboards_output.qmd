---
title: "Gapminder"
format: dashboard
theme: pulse
---

# Page 1

##

```{r}
library (tidyverse)
library(plotly) #for interactive graphs
library (reactable) #for interactive tables

#for highting information in the dashboard
library(bslib)
library(bsicons)
library(shiny)

#?value_box check more applications


p <- ggplot(cars, aes(speed, dist)) + geom_point()

ggplotly(p) #fit figure to screen used to visualise it
```


##



```{r}
reactable(iris)
```


##
```{r}
value_box(
  title = "customer lifetime value",
  value = "$5,000",
  showcase = bsicons::bs_icon("bank2"),
theme = 'teal'
)
  
```

```{r}
value_box(
  title = "customer lifetime value",
  value = "$5,000",
  showcase = bsicons::bs_icon("globe-europe-africa"),
theme = "primary"
)

#check for list of icons 
# https://icons.getbootstrap.com/
  
```

```{r}
value_box(
  title = "customer lifetime value",
  value = "$5,000",
  showcase = bsicons::bs_icon("bank2"),
theme = "success"
)
```

